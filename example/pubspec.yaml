name: suica_reader_example
description: Example app for the suica_reader package.
publish_to: "none"
version: 1.0.0

environment:
  sdk: ">=3.12.0 <4.0.0"
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter
  suica_reader:
    path: ../

dev_dependencies: 
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true